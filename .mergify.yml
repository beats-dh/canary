---
pull_request_rules:
  - name: Automatic merge on approval
    conditions:
      - "#approved-reviews-by>=0"
      - base=dev
      - label=pronto-pra-merge
    actions:
      merge:
        method: squash
   - name: delete head branch after merge
    conditions:
      - squash
    actions:
      delete_head_branch:
